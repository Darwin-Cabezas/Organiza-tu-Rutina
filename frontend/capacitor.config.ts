import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.organizaturutina.app',
  appName: 'Organiza tu Rutina',
  webDir: 'dist'
};

export default config;
